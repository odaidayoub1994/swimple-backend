import { User } from './user.entity';

/**
 * Root file that we add all the entities in the entities object
 * and export the entities object.
 */

const ENTITIES = {
  User,
};

export default ENTITIES;
